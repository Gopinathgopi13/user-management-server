'use strict';

// Removed — user_roles join table replaced by direct role_id FK on users table

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up() {},
  async down() {},
};
